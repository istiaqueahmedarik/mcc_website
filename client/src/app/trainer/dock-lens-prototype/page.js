import DockLensPrototype from './DockLensPrototype';

export const metadata = {
  title: 'WebGL Dock Lens Prototype | MCC',
  description: 'Isolated trainer dock lens prototype for interaction and visual review.',
};

export default function Page() {
  return <DockLensPrototype />;
}
